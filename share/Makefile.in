PREFIX	= $(bmake_PREFIX)
CFLAGS	= $(bmake_CFLAGS) -g -O2 
LDFLAGS = $(bmake_CLFLAGS)
CC	= $(bmake_CC)
LD	= $(bmake_LD)

all: bmake_all

check: test

test: bmake_test

bmake_all:

bmake_test:

install: bmake_install

clean: bmake_clean

maintainer-clean: clean
	$(bmake_clean)
	rm -f Makefile config.status sources.mk
#rm -rf $(DEPDIRS)

sinclude user.mk
sinclude sources.mk
sinclude src/deps/*

Makefile: Makefile.in configure sources
	@echo "rebuilding makefile"
	$(bmake_reconfigure)

Makefile.in:

sources:

configure: mktests/*
	@touch configure

dist:

.PHONY: all clean maintainer-clean check test dist
